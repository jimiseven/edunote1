- Prefers to communicate in Spanish (es); expects responses, summaries, and sub-agent prompts in Spanish as well. Confidence: 0.95
- Writes in informal Spanish with little punctuation: no accents, no capitalization, minimal or no periods, often run-on sentences separated by line breaks. Tolerance: 0.85
- Works on a local XAMPP stack (Windows, `C:\xampp\htdocs\edunote1\...`) for the edunote1 school-grades project (PHP/MySQL). Confidence: 0.7
- Cares about not altering the database when doing report/export changes; expects export/visual format changes to be read-only with respect to DB rows. Confidence: 0.75
- Do not run destructive UPDATE migrations that wipe historical column values (e.g. forcing `nota_extra = 0` for all rows) just because new code paths no longer use the column. Historical values may still feed business logic retroactively, and zeroing them silently breaks existing students' calculations. Confidence: 0.85
- When requirements involve business logic changes (e.g. grade computation rules), explicitly distinguishes between "modificaremos la base de datos" vs "solo en el sistema" — wants coordinated changes documented and the DB impact clearly called out (which tables/columns migrate, what happens to existing rows). Confidence: 0.8
- When a task description is ambiguous (especially around business rules, formulas, or edge cases), expects the assistant to ask structured clarifying questions BEFORE coding, presenting concrete options with a "(Recomendado)" recommendation rather than open-ended questions. When the user does not respond to the structured question but the recommended default is safe, proceeding with the recommendation is acceptable. Confidence: 0.85
- Thinks about the system in teacher/student terms (alumnos, parciales, trimestres, notas, promedios) rather than technical/DB terms; explanations and option labels should use domain language, not jargon. Confidence: 0.7
- For the edunote1 project, the grade model has areas (SER/SABER/HACER) with sub-indices, 3 parciales per trimestre, trimestrales = promedio_parciales + autoevaluacion (+ previously nota_extra), capped at 100, and 50 is the pass threshold. Any change to grade computation should respect these invariants unless told otherwise. Confidence: 0.75
- Prefers computation of derived/distributed values (like the nota_extra repartition across parciales) to happen at render time on each page load, rather than being materialized/migrated into the DB. The DB stores the original input (nota_extra) and the system recomputes the distribution live when displaying or exporting. Reinforced by an explicit user check confirming "no se modifica la base de datos directamente, solo mediante los helpers" — values flow through `repartirNotaExtra()` and similar pure helpers, never via DB-side migrations. Confidence: 0.85
- Uses database backups as the recovery strategy when destructive changes happen to historical data, and expects the system to be able to "pick up" restored values on the next page load without any manual re-run of migrations. Confidence: 0.65
- Maintains a per-branch changelog/README folder at `C:\xampp\htdocs\edunote1\logC\` containing txt files that document what was done in each branch/session (e.g. `README_decimales.txt` for the `decimales` branch). The README includes: the date of the session, concrete changes per file (behavior changes, business rules like reparto de nota_extra with 1/2/3 parciales multipliers and cap at 95), and DB-impact notes — so reviewers know what to validate. When opening or working on a branch, expect to create/update a corresponding `README_<rama>.txt` in `logC`. Can be reframed as a "log de lo que se hizo hoy" — daily/session log of changes done in that branch. Confidence: 0.85
