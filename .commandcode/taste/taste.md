- Prefers "yolo" mode: skip confirmations and proceed autonomously rather than asking before acting. Confidence: 0.9
- Communicates in Spanish and expects responses in Spanish. Confidence: 0.8
- Prefers reusing existing libraries and code patterns already present in the project rather than introducing new ones. Confidence: 0.8
- When improving/redesigning a UI, prefers preserving the existing color palette and design model/conventions and only reorganizing the layout for clarity. Confidence: 0.9
- Prefers data tables to use the maximum available screen space: full-height layout with a fixed top header (title, subtitle, search, buttons) and the table expanding to fill the remaining height with its own internal scroll and sticky column headers. Confidence: 0.7
- Prefers the agent to actually execute migrations and run end-to-end tests/verification of changes (e.g., run SQL against the local DB, simulate requests, verify results in the DB), rather than only writing code and describing manual validation steps. Confidence: 0.8
- Deploys to a cPanel production server and manages the production database there (e.g., applying SQL migrations via phpMyAdmin), with local development on XAMPP. Confidence: 0.7
- Prefers having step-by-step operational/deployment documentation written out as a text file saved in the relevant project folder (e.g., bds/) for future reference, rather than only explaining the steps in chat. Confidence: 0.7
- Prefers organizing dense/complex admin pages with tabs and other UI elements to minimize visible information and improve readability, rather than showing everything on one long page. Confidence: 0.4 (lowered: user asked to revert a tab-based redesign of an admin page, keeping the original numbered-section layout)
- Prefers database changes to be delivered as an executable SQL script in a `.txt` file (e.g., saved under `bds/`) that can be pasted/imported directly in phpMyAdmin, rather than only describing the migration steps in chat. Confidence: 0.8
- Prefers the agent to explicitly confirm the scope/impact of a change (what will and won't be affected), especially for database migrations, rather than assuming it is understood. Confidence: 0.7
- Prefers unfinished/beta features to be visibly labeled as beta (e.g., a "beta, aún no usar" badge) so they are not used before being validated. Confidence: 0.7
- Prefers maintaining a version-notes/changelog text file (e.g., cpnal.txt at the project root) that records the version, the GitHub branch being deployed, and only the approved changes. Confidence: 0.6
- Prefers SQL migration scripts to be idempotent and defensive: ordered so `CREATE TABLE` runs before any `UPDATE`, and indexes/constraints/foreign keys guarded conditionally so the script works whether the target table (or referenced tables) already exist or not. Confidence: 0.8
- Prefers SQL migration `.txt` files to be entirely valid SQL, with all instructions/notes/markers written as `--` comments, so the whole file can be pasted/imported directly into phpMyAdmin without syntax errors (plain-text markers like "FIN DEL SQL" or scope/instruction sections get copied along with the SQL and cause errors). Confidence: 0.8
